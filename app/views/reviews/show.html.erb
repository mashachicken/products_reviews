
<h3>Review for <%= @product.name %>:</h3>
<div class="review">
<h4><div class="w3-container w3-grey"><%= @review.author %> <h4>
  </div>
<h6> <span id="rating"> <%= @review.rating %>/5</span></h6>
<h5><%= @review.content_body %></h5>
</div>

<p><%= link_to "Edit review", edit_product_review_path(@product, @review) %></p>
<p><%= link_to "Delete review", product_review_path(@product, @review),
  :data => {:confirm => "Are you sure?",
  :method => "delete"} %></p>

  <p><%= link_to "Return to product", product_path(@product) %></p>
