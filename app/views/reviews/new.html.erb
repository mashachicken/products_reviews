<h1>Add a new review:</h1>

<%= form_for [@product, @review] do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :rating %>
  <%= f.text_area :rating %>
  <p><%= f.submit %></p>
<% end %>
